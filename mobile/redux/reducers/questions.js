const initialState = {
  questions: {
    1: 'Est ce que la Croix-Rouge est géniale ?',
  },
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
